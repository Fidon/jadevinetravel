(function ($) {
  "use strict";

  // The scroll reveal and load reveal systems in main.js handle
  // all animation on this page automatically — no reinit needed.
  // This file exists as the correct entry point for any future
  // about-page-specific interactions (lightbox, team modal, etc.)

  // Active nav link — mark "About" as active
  //   $(document).ready(function () {
  //     $('.jd-nav-link').each(function () {
  //       var href = $(this).attr('href') || '';
  //       if (href && window.location.pathname.indexOf(href) !== -1 && href !== '/') {
  //         $(this).addClass('active');
  //       }
  //     });
  //   });
})(jQuery);
